# KiCad local state — user-specific, never shared
*.kicad_prl
*-backups/
fp-info-cache
_autosave-*

# Reports regenerated by `make schematic` / `make board`
*-erc.rpt
*-drc.rpt

# Renders — regenerated by `make render`, never reviewed as a diff
render/
